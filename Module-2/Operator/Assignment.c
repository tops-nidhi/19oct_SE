// WAP to use of assignment operator
#include <stdio.h>
int main()
{
    int x, y;
    // a+=b -> a = a+b -> a = 5 + 2 -> a = 7
    // a-=b -> a = a-b -> a = 7 - 2 -> a = 5
    // b*=b -> b = b*b -> b = 2 * 2 -> b = 4
    // +=, -=, /=, *=, %=
    return 0;
}